---
engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript
  eslint:
    enabled: true
    config:
      .eslintrc
    channel: "eslint-3"
  fixme:
    enabled: true
ratings:
  paths:
    - src/**/*.js
exclude_paths:
  - tests/
